a <- 10
# Ctrl + Enter: 커서가 있는 현재 라인을 실행.
# 주석(comment): 소스 코드에 설명을 작성하는 것.
# 실행되지 않는 코드.
b <- 123

# 사칙연산
a + b
a - b
a * b
a / b
b / a

# plus 변수에 a + b의 계산 결과를 저장
plus <- a + b
# plus 변수의 값을 출력
plus
